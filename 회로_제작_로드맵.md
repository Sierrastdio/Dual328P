# DST 듀얼코어 시스템 - 실전 제작 & 검증 로드맵

## 📋 준비 물품 체크리스트

```
필수:
□ 멀티미터 (저항, 연속성 테스트)
□ 브래드보드 또는 프로토타입 기판
□ 점프 와이어 (검은색/빨간색 구분)
□ USB 시리얼 어댑터 (또는 아두이노 우노 이용)
□ 로직 애널라이저 (강력 권장, $30~100)

선택:
□ 오실로스코프 (신호 파형 확인)
□ 전원 공급기 (정확한 5V, 1A 이상)
□ 저항/캐패시터 키트 (풀업, 바이패스용)
```

---

## 🔧 제작 순서: 5단계 피라미드 구조

### **STAGE 1: 전원부 & 기본 인프라 (2~3시간)**

목표: 안정적인 전원 공급과 기본 신호 확인

```
구성 요소:
├─ 전원 レール 구축
│  ├─ +5V (아두이노 또는 USB 파워뱅크)
│  └─ GND (공통)
├─ 바이패스 커패시터 설치
│  └─ 각 IC 근처에 0.1μF 세라믹 캡 (하이패스)
└─ 테스트 LED + 100Ω 저항
   └─ +5V → LED → GND (전원 확인용)
```

**검증 방법:**
```
✓ 멀티미터로 VCC-GND 전압 = 5.0±0.2V 확인
✓ LED 밝기 확인
✓ 선연결 쇼트 테스트 (멀티미터 연속성 모드)
```

---

### **STAGE 2: Core 1 최소 구성 (4~5시간)**

목표: 한 개의 마이크로컨트롤러만으로 기본 동작 확인

```
추가 컴포넌트:
├─ ATmega328P (Core 1)
│  ├─ 크리스탈 진동자 (16MHz)
│  ├─ 2개의 22pF 캐패시터 (크리스탈 양쪽)
│  ├─ 10kΩ 풀업 저항 (RESET)
│  └─ ICSP 핀 헤더 (프로그래밍용, 선택사항)
├─ LED x 8개 + 100Ω 저항 x 8
│  └─ PD2~PD7, PB0~PB1에 각각 연결
│     (데이터 버스 시각화)
└─ 1개 버튼 + 10kΩ 풀다운
   └─ PC5에 연결 (간단한 입력 테스트)
```

**배선 순서 (반드시 이 순서):**
```
1) 전원과 GND 핀들을 먼저 연결
2) RESET 회로 (풀업 저항 + 캡)
3) 크리스탈 + 2개 22pF 캡 (XTAL1, XTAL2)
4) LED 배열 (PD2~PD7, PB0~PB1)
5) 버튼 (PC5)
```

**테스트 펌웨어 (core1_stage2.ino):**

```cpp
#include <avr/io.h>
#include <util/delay.h>

void setup() {
    // PD, PB 모두 OUTPUT으로 설정
    DDRD = 0xFF;  // PD0~PD7 모두 출력
    DDRB = 0xFF;  // PB0~PB1 출력
    DDRC = 0x00;  // PC 모두 입력 (PC5만 사용)
}

void loop() {
    // 테스트 1: 간단한 LED 점멸
    PORTD = 0xFF;  // D 포트 켜기
    PORTB = 0x03;  // B0, B1 켜기
    _delay_ms(500);
    
    PORTD = 0x00;  // D 포트 끄기
    PORTB = 0x00;  // B0, B1 끄기
    _delay_ms(500);
}
```

**검증:**

| 단계 | 검증 방법 | 확인 항목 |
|------|---------|---------|
| 1단계 | 멀티미터 | RESET 핀 = ~5V, 크리스탈 주변 바이패스 캡 설치 |
| 2단계 | 육안 + 멀티미터 | 8개 LED 모두 동시에 깜빡이는가? |
| 3단계 | 로직 애널라이저 | PD2~7, PB0~1 신호가 일제히 HIGH→LOW 변환되는가? |
| 4단계 | 버튼 테스트 | 버튼 누를 때 LED 밝기가 변하는가? (입력 확인) |

---

### **STAGE 3: 데이터 버스 인터페이스 추가 (3~4시간)**

목표: 버퍼 IC (74HC245)를 통한 데이터 통신 테스트

```
추가 컴포넌트:
├─ 74HC245 (데이터 버퍼) x 2개
│  ├─ A 측: ATmega328P (Core 1)에서 나오는 신호
│  └─ B 측: 향후 EEPROM 또는 다른 IC로 갈 신호
├─ LED x 8개 (B 측 출력 시각화)
└─ 조정 가능 풀업 저항 (방향 제어용)
```

**배선 시 주의:**

```
74HC245의 방향 제어:
┌─────────────────────────────────────┐
│ 핀 1 (DIR): LOW = A→B 방향         │
│           HIGH = B→A 방향          │
│ 핀 19 (OE#): LOW = 활성화          │
│            HIGH = 비활성화(고임피던스) │
└─────────────────────────────────────┘

Core 1 연결:
- PORTD (PD2~7) → 74HC245 A0~A5
- DIR = HIGH에 고정 (Core1 → Buffer 사용)
- OE# = GND (항상 활성화)
```

**테스트 펌웨어 (core1_stage3.ino):**

```cpp
void loop() {
    // 주소 버스처럼 8비트 값 변화
    for (uint8_t i = 0; i < 256; i++) {
        PORTD = i & 0xFF;  // 0x00 → 0xFF로 증가
        _delay_ms(100);
    }
}
```

**검증:**

| 항목 | 검증 방법 | 예상 결과 |
|------|---------|---------|
| 전원 | 멀티미터 | VCC=5V, GND=0V |
| 신호 전파 | 로직 애널라이저 | 입력(A) vs 출력(B)이 동일한가? |
| 타이밍 | 오실로스코프 | 신호 전파 지연 <2μs |
| 양방향 | DIR 핀 토글 | 방향 변경 시 신호 역전 확인 |

---

### **STAGE 4: 메모리 제어 시스템 추가 (5~6시간)**

목표: 74HC595 시프트 레지스터로 페이징 제어

```
추가 컴포넌트:
├─ 74HC595 (시프트 레지스터) x 2개
│  ├─ Core 1 PD0 (SER) → 74HC595 #1 pin 14
│  ├─ Core 1 PD1 (SCK) → 74HC595 #1 pin 11
│  └─ Core 1 PC3 (RCK) → 74HC595 #1 pin 12
└─ LED x 8개 (QA~QH 출력 표시)
```

**시프트 레지스터 작동 원리:**
```
Core 1에서 8비트를 순차 전송:
PD0 (SER) ──→ [비트 시프팅] ──→ PD1 (SCK로 클록) ──→ PC3 (RCK로 래치)

예: 0b10101010 입력
PORTD |= (1<<0);      // SER=1 (첫 비트)
PORTD |= (1<<1);      // SCK 펄스
PORTD &= ~(1<<1);
PORTD &= ~(1<<0);     // SER=0 (두 번째 비트)
(반복...)
PORTC |= (1<<3);      // RCK 펄스 → 모든 비트 동시 출력
```

**테스트 펌웨어 (core1_stage4.ino):**

```cpp
void shift_register_write(uint8_t value) {
    // 74HC595에 8비트 순차 입력
    for (int i = 7; i >= 0; i--) {
        // SER (PD0)에 비트 입력
        if (value & (1 << i)) {
            PORTD |= (1 << 0);   // SER = 1
        } else {
            PORTD &= ~(1 << 0);  // SER = 0
        }
        
        // 클록 펄스 (PD1)
        PORTD |= (1 << 1);   // SCK = 1
        _delay_us(1);
        PORTD &= ~(1 << 1);  // SCK = 0
        _delay_us(1);
    }
    
    // 래치 펄스 (PC3) - 모든 비트를 Q 출력으로 전송
    PORTC |= (1 << 3);   // RCK = 1
    _delay_us(1);
    PORTC &= ~(1 << 3);  // RCK = 0
}

void loop() {
    for (uint8_t pattern = 0; pattern < 256; pattern++) {
        shift_register_write(pattern);
        _delay_ms(100);
    }
}
```

**검증 (로직 애널라이저 필수):**

```
신호 분석:
┌────────────────────────────┐
│ SER (PD0)  : ▬▼▬▬▼▬▬▼▬▬...  │ (8비트 순차)
│ SCK (PD1)  : ▬▲▼▬▲▼▬▲▼...    │ (8회 클록)
│ RCK (PC3)  : ▬────▲▼────    │ (8비트 후 1회 래치)
│ QA~QH      : XXXX▬────...   │ (RCK 후 안정화)
└────────────────────────────┘

확인 사항:
✓ SER 신호가 정확히 8비트 보이는가?
✓ SCK는 8번 정확히 펄싱하는가?
✓ RCK 펄스 후 출력이 변하는가?
✓ 타이밍: SCK 주기 = 2~10μs (Core 1의 속도에 따라)
```

---

### **STAGE 5: 28C256 EEPROM 통합 (6~8시간)**

목표: 완전한 메모리 시스템 (주소 + 데이터 + 페이징)

```
최종 컴포넌트 배치:
├─ ATmega328P Core 1
├─ 74HC245 x 2 (데이터 버퍼)
│  ├─ #1: Core 1 데이터 출력
│  └─ #2: EEPROM ↔ Core 통신
├─ 74HC595 x 2 (페이징)
│  ├─ #1: Core 1 → A7~A13 페이징
│  └─ #2: Core 2 → A7~A13 페이징
├─ 28C256 EEPROM (16KB, A0~A13)
│  ├─ A0~A6: 74HC245 주소 출력
│  ├─ A7~A13: 74HC595 QA~QG
│  └─ D0~D7: 74HC245 데이터
├─ 주소 래치 (선택사항, 타이밍 안정화)
└─ 제어 신호
   ├─ WE# (쓰기 허용)
   ├─ OE# (읽기 허용)
   └─ CE# (칩 선택)
```

**메모리 맵:**

```
28C256 16KB 구성:
┌──────────┬────────────┐
│ 페이지 # │ 주소 범위  │
├──────────┼────────────┤
│ Page 0   │ 0x0000~0x7F │
│ Page 1   │ 0x0080~0xFF │
│ ...      │ ...        │
│ Page 127 │ 0x3F80~3FFF │
└──────────┴────────────┘

현재 페이지 = 74HC595 (QA~QG) 값
현재 주소 = PB2~5 + PC0~2 (0~127) + 페이지

예: Page=5, Addr=50
   → EEPROM 물리 주소 = (5 << 7) | 50 = 0x2A0
```

**테스트 펌웨어 (core1_stage5.ino):**

```cpp
#define EEPROM_WE   0  // PA0 = WE#
#define EEPROM_OE   0  // PA1 = OE#
#define EEPROM_CE   0  // PA2 = CE#

void set_address(uint16_t addr) {
    // 7비트 주소 (PB2~5, PC0~2)
    uint8_t lower = addr & 0x7F;
    PORTB = (PORTB & 0x03) | ((lower & 0x0F) << 2);
    PORTC = (PORTC & 0xF8) | ((lower >> 4) & 0x07);
    
    // 7비트 페이지 (74HC595)
    uint8_t page = (addr >> 7) & 0x7F;
    shift_register_write(page);
}

void write_byte(uint16_t addr, uint8_t data) {
    set_address(addr);
    PORTD = data;
    
    // WE# 펄스
    // (구체적 포트는 실제 배선에 따라 조정)
    _delay_us(1);
    _delay_us(1);
}

uint8_t read_byte(uint16_t addr) {
    set_address(addr);
    
    // OE# 활성화 + 읽기
    DDRD = 0x00;  // 입력 모드
    _delay_us(1);
    uint8_t data = PIND;
    DDRD = 0xFF;  // 다시 출력 모드
    
    return data;
}

void loop() {
    // 메모리 쓰기 테스트
    for (uint16_t i = 0; i < 256; i++) {
        write_byte(i, i & 0xFF);
    }
    
    // 메모리 읽기 검증
    for (uint16_t i = 0; i < 256; i++) {
        uint8_t readVal = read_byte(i);
        // Serial로 검증 스트림 전송
    }
}
```

**검증:**

| 단계 | 검증 방법 | 확인 내용 |
|------|---------|---------|
| 주소 설정 | 로직 애널라이저 | A0~A13 모두 정확히 출력되는가? |
| 데이터 입력 | 멀티미터 | D0~D7이 올바른 값을 가지는가? |
| WE# 펄스 | 오실로스코프 | 쓰기 펄스 너비 = 100ns~1μs |
| 데이터 읽기 | Serial 모니터 | 쓴 값과 읽은 값이 동일한가? |
| 페이징 | 로직 분석 | A7~A13 변경 후 메모리 다른 영역 접근 |

---

### **STAGE 6: Core 2 추가 & 핸드셰이크 (4~5시간)**

목표: 듀얼코어 동작 확인

```
추가 마이크로컨트롤러는 Core 1과 동일하게 구성
차이점:
├─ 74HC595 #2 (다른 시프트 레지스터)
├─ 74HC245 #2 데이터 버퍼
└─ PC5: Core 1 PC5와 직접 연결 (핸드셰이크)
```

**핸드셰이크 프로토콜:**

```cpp
// Core 1
void wait_for_core2() {
    while ((PINC & (1 << 5)) == LOW) {
        // Core 2가 작업 중... 대기
    }
}

PORTC &= ~(1 << 5);  // Core 2에 작업 요청
wait_for_core2();     // 완료 대기
// ...여기서 Core 2의 결과 읽기

// Core 2
ISR(PCINT1_vect) {
    if ((PINC & (1 << 5)) == LOW) {
        // Core 1의 요청 감지
        do_work();
        PORTC |= (1 << 5);  // 완료 신호
    }
}
```

---

## 🔍 각 STAGE별 예상 시간 & 어려움도

```
STAGE 1: 2~3h   ████░░░░░░  (쉬움, 펜치/멀티미터)
STAGE 2: 4~5h   ████████░░  (중간, 프로그래밍 필요)
STAGE 3: 3~4h   ██████░░░░  (중간, 신호 분석)
STAGE 4: 5~6h   ████████░░  (높음, 로직 분석 필수)
STAGE 5: 6~8h   ██████████  (매우 높음, EEPROM 타이밍)
STAGE 6: 4~5h   ████████░░  (높음, 위험: 서로 간섭)

총 소요 시간: 24~31시간 (프로토타입 기준)
```

---

## 🛟 문제 발생 시 원인 추적 플로우

```
현상: LED가 깜빡이지 않음
├─ Step 1: 멀티미터로 VCC/GND 확인
├─ Step 2: 크리스탈 근처 신호 → 로직 애널라이저
├─ Step 3: RESET 핀 논리 레벨
└─ Step 4: 펌웨어 재업로드, FUSES 확인

현상: 일부 LED만 작동
├─ Step 1: 해당 핀들을 멀티미터로 테스트
├─ Step 2: 로직 애널라이저로 타이밍 확인
├─ Step 3: 배선 재검토 (단락 확인)
└─ Step 4: 해당 핀의 DDR/PORT 초기화 재확인

현상: 메모리 쓰기/읽기 실패
├─ Step 1: 주소 버스 (A0~A13) 신호 확인
├─ Step 2: 데이터 버스 (D0~D7) 신호 확인
├─ Step 3: WE#, OE#, CE# 제어 신호 타이밍
└─ Step 4: EEPROM 칩 자체 교체 테스트

현상: 두 코어 간 통신 불안정
├─ Step 1: 핸드셰이크 신호 (PC5) 확인
├─ Step 2: 각 코어를 독립적으로 테스트
├─ Step 3: 동시성 문제 → 지연 추가
└─ Step 4: 신호 무결성 (풀업/풀다운 조정)
```

---

## 📊 점검표 (인쇄하고 체크하세요)

```
STAGE 1 완료?
□ LED 점멸 O
□ 전원 전압 안정 O
□ 멀티미터 연속성 테스트 O

STAGE 2 완료?
□ Core 1 프로그램 업로드 O
□ 6개 데이터 버스 LED 작동 O
□ 로직 애널라이저 신호 확인 O
□ 버튼 입력 동작 O

STAGE 3 완료?
□ 74HC245 배선 확인 O
□ 입/출력 신호 동일성 O
□ 양방향 전환 작동 O

STAGE 4 완료?
□ 시프트 레지스터 신호 타이밍 O
□ 8개 출력 패턴 확인 O
□ RCK 래칭 작동 O

STAGE 5 완료?
□ EEPROM 주소 모두 출력 O
□ 데이터 쓰기 작동 O
□ 데이터 읽기 검증 O
□ 페이징 전환 작동 O

STAGE 6 완료?
□ Core 2 독립 작동 O
□ PC5 핸드셰이크 신호 O
□ 서로 간섭 없음 O
□ 메모리 공유 작동 O
```

---

## 💡 Pro Tips

1. **스냅샷 기록**: 각 STAGE 완료 후 휴대폰으로 배선도 촬영
2. **라벨링**: 와이어와 핀에 숫자/색상 라벨 부착
3. **점진적 복잡화**: 절대 한 번에 모든 배선 설치하지 말 것
4. **테스트 코드 보관**: `examples/` 폴더에 각 STAGE별 코드 저장
5. **로직 애널라이저 활용**: $50짜리라도 극도로 가치있음
6. **신호 무결성**: 길이 >10cm 배선은 저항/capacitor로 종단 처리 검토
